import 'package:flutter/material.dart';

class VitalsContainer extends StatelessWidget {
  const VitalsContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
